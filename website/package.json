{
    "name": "synapse-website",
    "type": "module",
    "version": "1.0.0",
    "scripts": {
        "sync": "bun scripts/sync-data.ts",
        "clean": "bun scripts/clean-duplicates.ts",
        "clean:dry": "bun scripts/clean-duplicates.ts --dry-run",
        "dev": "astro dev",
        "start": "bun run dev",
        "build": "astro build",
        "preview": "astro preview",
        "astro": "astro"
    },
    "dependencies": {
        "@notionhq/client": "^2.2.14",
        "@vercel/analytics": "^1.6.1",
        "astro": "^4.0.0",
        "firebase": "^12.7.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0"
    }
}